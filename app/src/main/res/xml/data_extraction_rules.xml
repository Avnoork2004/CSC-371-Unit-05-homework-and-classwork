<?xml version="1.0" encoding="utf-8"?><!--
   Sample data extraction rules file; uncomment and customize as necessary.
   See https://developer.android.com/about/versions/12/backup-restore#xml-changes
   for details.
-->
<data-extraction-rules>
    <cloud-backup>

        <!-- TODO: Use <include> and <exclude> to control what is backed up.-->

        <!-- Includes all files in app's files directory -->
        <include domain="file" path="." />

        <!-- Excludes specific file -->
        <exclude domain="file" path="exclude_this_file.txt" />

        <!-- Includes all shared preferences -->
        <include domain="sharedpref" path="." />

        <!-- Excludes specific shared preference file -->
        <exclude domain="sharedpref" path="exclude_this_pref.xml" />

    </cloud-backup>
    <device-transfer>

        <!-- Includes all files in app's files directory -->
        <include domain="file" path="." />

        <!-- Excludes specific file -->
        <exclude domain="file" path="exclude_this_file.txt" />

        <!-- Includes all shared preferences -->
        <include domain="sharedpref" path="." />

        <!-- Excludes specific shared preference file -->
        <exclude domain="sharedpref" path="exclude_this_pref.xml" />

    </device-transfer>
</data-extraction-rules>